# Project Summary
ویدارا یک SaaS چندمستاجری مدیریت جلسات است که فایل‌های جلسه (صوت/ویدیو و پیوست‌ها) را به صورتجلسهٔ ساختارمند تبدیل می‌کند: آپلود مستقیم با نشانی امضاشده، جداسازی خودکار صدای ویدیو با ffmpeg، رونویسی فارسی با «حرف» (Roshan AI)، پیش‌نویس صورت‌جلسه و مصوبات/اقدامات با DeepSeek، گردش تأیید و قفل، جست‌وجوی تمام‌متن، خروجی Word/ICS، آرشیو روی استوریج خارجی (S3/WebDAV)، اعلان ایمیل/پیامک و پنل مدیریت پلتفرم. کارهای پردازشی روی جدول پایدار `jobs` به‌صورت in-process اجرا می‌شوند (بدون Celery/Redis).

# Project Module Description
## Frontend (React + TypeScript + Vite + shadcn/ui — RTL)
- صفحات: `src/pages/Login.tsx`، `CompleteProfile.tsx`، `Dashboard.tsx`، `Meetings.tsx`، `MeetingDetail.tsx`، `Settings.tsx`، `Account.tsx`، `PrintMinutes.tsx`، `PlatformAdmin.tsx` (پنل پلتفرم) و `blog/`
- کامپوننت‌ها: `AppShell.tsx` (پوستهٔ فضای کاری)، `PlatformShell.tsx` (پوستهٔ پلتفرم)، `AssistantPanel.tsx` (دستیار هوشمند)، `AiUsagePanel.tsx` (توکن ویدارا)، `JalaliDateTimePicker.tsx`، `MarkdownText.tsx`، `HighlightText.tsx`، `MeetingAttachmentsCard.tsx`، `OrganizationSwitcher.tsx`، `ErrorBoundary.tsx`، `LoadingGif.tsx`، `VidaraBranding.tsx` و پنل‌های `components/settings/`
- کلاینت‌های API: `src/lib/mgmt.ts` (فضای کاری)، `appAuth.ts` (ورود/کاربران)، `platform.ts` (پلتفرم)، `assistant.ts`، `aiSettings.ts`، `notify.ts`، `session.ts`، `utils.ts` (نرمال‌سازی فارسی و برجسته‌سازی)

## Backend (FastAPI async + SQLAlchemy 2 + PostgreSQL)
- روترهای فضای کاری: `workspace.py` (جلسات/دستور/شرکت‌کنندگان/صورتجلسه/مصوبات/اقدامات/داشبورد + جست‌وجوی تمام‌متن)، `meeting_ai.py` (آپلود صوت/ویدیو، کارهای رونویسی/پیش‌نویس/جداسازی صدای ویدیو، گوینده‌ها، ترتیب فایل‌ها)، `minutes_flow.py` (گردش تأیید و خروجی Word/JSON)، `assistant.py`، `archive.py`، `meeting_attachments.py`، `notify_settings.py`، `ai_settings.py`، `settings.py`، `runtime_config.py`
- روتر پلتفرم: `platform.py` — سازمان‌ها و مدیران، تنظیمات هر سازمان (ایمیل/پیامک/AI/استوریج/سقف‌ها)، مصرف AI کل پلتفرم، سطل آشغال و پاک‌سازی کامل
- احراز هویت: `app_auth.py` — ثبت‌نام/ورود/کاربران/تکمیل اجباری مشخصات/تغییر فضای کاری/حذف سازمان؛ مدیر پلتفرم با توکن `typ=vidara_platform`
- سرویس‌ها: `ai_providers.py` (زنجیرهٔ STT/LLM با اولویت و fallback؛ آداپتور «حرف»)، `ai_usage.py` (توکن ویدارا + تعرفهٔ روز دیپ‌سیک)، `assistant.py`، `meeting_speakers.py`، `minutes_docx.py` (Word راست‌به‌چپ)، `minutes_settings.py`، `meeting_archive.py`/`external_storage.py`، `notify_channels.py`، `upload_limits.py`، `mgmt_core.py` (`fa_normalize` و `TenantContext`)
- مدل‌ها: `models/*` — همهٔ جداول دامنه با `organization_id`؛ `jobs` (صف پایدار)، `recordings` (با `position`/`media_kind`/`video_*`)، `transcripts`، `meeting_speakers`، `platform_admins`، `ai_user_usage` و …

## Deploy/Operations
- `deploy/docker-compose.yml`: db (PostgreSQL)، minio، oss-gateway، backend، frontend، proxy (nginx)؛ فقط proxy پورت میزبان منتشر می‌کند (`APP_PORT`/`STORAGE_HOST_PORT`، پیش‌فرض ۷۰۸۰/۷۴۴۳)
- `deploy/nginx/`: قالب‌های HTTP/HTTPS با گواهی دستی در `deploy/nginx/certs` (یا خاتمهٔ TLS در لبه)
- `deploy/scripts/`: init-env، install، init-storage، issue-ssl، backup، restore، update، status، logs
- ایمیج بک‌اند: pip کاملاً آفلاین از `app/backend/.wheels/` + ffmpeg/ffprobe استاتیک در `app/backend/bundle/bin/`

# Directory Tree
- `app/frontend/src/` — `pages/`، `components/` (+`ui/`، `settings/`، `blog/`)، `lib/`
- `app/backend/` — `routers/`، `services/`، `models/`، `core/`، `dependencies/`، `schemas/`، `main.py`، `Dockerfile`، `bundle/`، `.wheels/`
- `deploy/` — `docker-compose.yml`، `.env.example`، `nginx/`، `scripts/`، `oss-gateway/`
- `docs/` — `architecture.md`، `deployment.md`، `quickstart.md`، `mvp_feature_review.md`، `test-scenario.md`
- `.notes/` — `ARCHITECTURE.md`، `ATOMS.md` (تصمیم‌ها)، `PROGRESS.md` (پیشرفت)
- `uploads/` — مستندات مرجع سرویس «حرف» و پنل پیامک

# File Description Inventory
- `app/backend/routers/workspace.py` — جلسات/دستور/RSVP/حضور/صورتجلسه/مصوبات/اقدامات/داشبورد؛ جست‌وجوی تمام‌متن با `search_scope` و بندهای یافت‌شده (`matches`)
- `app/backend/routers/meeting_ai.py` — upload-url/register برای صوت و ویدیو، ترتیب فایل‌ها (`PUT /recordings/order`)، کارهای `transcribe`/`minutes_draft`/`audio_extract`، جداسازی صدا با ffmpeg، گوینده‌ها و کلیپ صدا، حذف ویدیو با نگه‌داری صدا
- `app/backend/routers/platform.py` — پنل مدیریت پلتفرم شامل `GET /ai-summary` (مصرف حرف/دیپ‌سیک + دلار نرخ روز) و `ai_usage` در `GET /orgs/{id}/activity`
- `app/backend/services/ai_usage.py` — واحد یکپارچهٔ «توکن ویدارا» (۱ دقیقه STT = ۱ توکن، ۱ سنت LLM = ۱ توکن) و تعرفهٔ روز دیپ‌سیک
- `app/backend/services/ai_providers.py` — کاتالوگ STT/LLM، اعتبار پیش‌فرض حرف/دیپ‌سیک از env، زنجیرهٔ اولویت/fallback
- `app/backend/services/mgmt_core.py` — `TenantContext`/`resolve_context`/`list_owned`/`get_owned` (مرز مستأجر در کد)، `fa_normalize`، اعتبارسنجی صوت/ویدیو
- `deploy/oss-gateway/app.py` — دروازهٔ MinIO با `OSS_API_KEY` برای presign و CRUD باکت/شیء
- `deploy/docker-compose.yml` — زیرساخت کامل با انتشار تنها پورت‌های proxy

# Technology Stack
- Backend: FastAPI (async) + SQLAlchemy 2 + PostgreSQL 16 (create_all + مهاجرت‌های ALTER دستی)
- کارهای پس‌زمینه: جدول `jobs` + اجرای in-process (`asyncio.create_task`)، بدون Celery/Redis
- Storage: MinIO خصوصی (presigned)؛ استوریج خارجی S3-compatible/WebDAV برای آرشیو
- AI: رونویسی «حرف» (Roshan AI)، مدل زبانی DeepSeek، زنجیرهٔ اولویت با fallback
- Frontend: React 18 + TypeScript + Vite 5 + shadcn/ui + Tailwind (RTL فارسی)
- Deploy: Docker + Docker Compose، nginx reverse proxy، گواهی دستی یا TLS در لبه

# Usage
## Deployment (Docker)
1) پوشه `deploy` را آماده کنید: `.env` از روی `deploy/.env.example` یا با `bash scripts/init-env.sh`.
2) استقرار کامل: `bash scripts/install.sh`
3) عملیات‌های نگهداری:
- وضعیت: `bash scripts/status.sh`
- لاگ‌ها: `bash scripts/logs.sh [backend|frontend|proxy|...]`
- بکاپ: `bash scripts/backup.sh` / بازیابی: `bash scripts/restore.sh <path-to-tar.gz>`
- به‌روزرسانی: `bash scripts/update.sh`

## Local development
- فرانت: `cd app/frontend && pnpm install && pnpm run dev` (بیلد: `pnpm run build` شامل `tsc --noEmit`)
- بک‌اند: `cd app/backend && pip install -r requirements.txt && uvicorn main:app --reload`
- مستندات تکمیلی: `docs/quickstart.md` (نصب سریع)، `docs/deployment.md` (جزئیات عملیاتی)
