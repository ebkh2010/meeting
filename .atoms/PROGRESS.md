---
last_updated: 2026-08-25T03:54:54Z
---

# Requirements & Progress

## Requirements Overview

## User Stories

## Task Breakdown
- [x] داکرایز بک‌اند با ffmpeg و فونت‌های فارسی + healthcheck و اجرا پشت proxy
- [x] داکرایز فرانت به‌صورت چندمرحله‌ای (ساخت با Node، سرو استاتیک با nginx و fallback مسیرهای SPA)
- [x] دروازهٔ ذخیره‌سازی مستقل روی MinIO با همان قرارداد ObjectStorage پروژه (بدون تغییر کد قابلیت‌ها)
- [x] docker-compose با انتشار تنها پورت‌های ۸۰/۴۴۳ و volume ماندگار برای پایگاه داده، فایل‌ها و گواهی‌ها
- [x] reverse proxy با دو دامنه (سامانه و فایل‌ها)، هدرهای امنیتی، سقف حجم بارگذاری و مسیر چالش ACME
- [x] گواهی SSL با Let's Encrypt (صدور با webroot + تمدید خودکار هر ۱۲ ساعت)
- [x] انتقال آدرس پایهٔ API به متغیر محیطی با endpoint `GET /api/config` (تست واقعی: پاسخ خالی و مقدار محیطی)
- [x] اسکریپت‌های نصب/کلیدسازی/باکت/SSL/پشتیبان/بازیابی/به‌روزرسانی/وضعیت/لاگ با پیام‌های فارسی
- [x] گارد کلیدهای حیاتی در نصب: خالی نبودن، طول حداقل ۱۶ نویسه و رد مقادیر نمونه
- [x] مستند فارسی استقرار در `docs/deployment.md` (پیش‌نیاز، معماری، عملیات، پشتیبان، عیب‌یابی، امنیت)
- [x] اعتبارسنجی واقعی بستهٔ استقرار: `bash -n` همهٔ اسکریپت‌ها، صحت YAML و پورت/volume، `py_compile`، تست روتر پیکربندی، `pnpm run lint` و `pnpm run build`
- [x] ساخت ۱۶ جدول دامنه با `organization_id` روی پایگاه دادهٔ پلتفرم
- [x] ساخت باکت خصوصی صوت جلسه و تنظیم محدودیت حجم/فرمت
- [x] هستهٔ بک‌اند: TenantContext، RBAC سه نقش با ۴۰۳، QuotaGuard، Audit، اعلان، seed دادهٔ نمایشی
- [x] روتر اصلی: سازمان/اعضا/دعوت/جلسه/دستور جلسه/RSVP/حضور/صورتجلسه/مصوبات/اقدامات/داشبورد/اعلان/Audit/کنسول ادمین/ICS
- [x] لایهٔ AI با الگوی Port/Adapter (آداپتر پلتفرم فعال + آداپتر «حرف» غیرفعال) و روتر کار غیرهمزمان با jobs + polling + retry بدون هزینهٔ دوباره
- [x] فرانت‌اند: لایهٔ API، تبدیل تاریخ شمسی، پوستهٔ RTL و ناوبری
- [x] صفحات: ورود/معرفی، داشبورد، جلسات، جزئیات جلسه (صوت/رونویسی/صورتجلسه/مصوبات)، اقدامات، اعضا، کنسول ادمین، نمای چاپ PDF
- [x] اعتبارسنجی نهایی: lint، build، بررسی رندر UI و ثبت گزارش
- [x] بازطراحی سیستم بصری (توکن‌های رنگ/تم/تایپوگرافی) در `index.css` با الهام از مرجع مدرن SaaS
- [x] بازطراحی کامل لندینگ: قاب اول سینمایی، ویژگی‌ها، نحوهٔ کار، اعتماد، پلن‌ها و CTA
- [x] بهبود پوستهٔ اپ: هدر چسبان، برندینگ، ناوبری و هم‌ترازی RTL
- [x] حذف وابستگی به دادهٔ نمایشی: endpoint `POST /api/v1/workspace/demo-data/purge` + پنل پاک‌سازی در کنسول مدیریت با تأیید عبارتی
- [x] برندینگ سفارشی ویدارا: کامپوننت مشترک لوگو+عبارت «توسعه داده شده توسط تیم ویدارا. نسخه آزمایشی» در فوتر لندینگ/فوتر فضای کاری/نمای چاپ و پاک‌سازی کامل متادیتا و favicon و og/twitter از برندینگ پیشین
- [x] احراز هویت مستقل بک‌اند: جدول کاربران سازمانی، ثبت‌نام/ورود/تغییر رمز، توکن `X-App-Token`، RBAC سه‌نقشی و اعمال مرز مستأجر در همهٔ روترها
- [x] سیستم اعلان جلسه: تنظیمات SMTP و پیامک قاصدک، متن دعوت با تاریخ شمسی و پیوست ICS، لاگ ارسال‌ها و ارسال دوباره
- [x] اتصال ارسال دعوت به ایجاد جلسه پس از commit و ثبت نتیجه در گزارش ارسال‌ها
- [x] فرانت‌اند احراز هویت مستقل: صفحهٔ ورود/ثبت‌نام در ریشه، پروفایل و تغییر رمز، مدیریت کاربران سازمان، صفحهٔ تنظیمات اعلان با تست ارسال
- [x] حذف کامل لندینگ و مسیرهای احراز هویت پلتفرم؛ ریشهٔ `/` به صفحهٔ ورود مستقل تغییر یافت و پوستهٔ برنامه با نگهبان نشست بازنویسی شد
- [x] یکدست‌سازی نام محصول در متادیتای صفحه، عنوان پیش‌فرض و لایهٔ بلاگ
- [x] بهبود کشف‌پذیری مدیر: بخش «شروع سریع» در داشبورد، منوی میان‌بر حساب در هدر و برچسب‌های گویای ناوبری
- [x] بخش یکپارچهٔ «تنظیمات» با آیکون هدر مخصوص مدیر (کاربران/نقش‌ها + ایمیل SMTP + پیامک قاصدک + گزارش ارسال) و رد دسترسی ۴۰۳ برای دبیر/عضو
- [x] رفع عنوان و آیکون زبانهٔ مرورگر: تثبیت برند «ویدارا — نسخه جلسات» در برابر متغیرهای محیطی پلتفرم و فاویکون اختصاصی SVG
- [x] رفع باگ بحرانی نقش مدیر: یکسان‌سازی کلید فنی نقش (`org_admin`) میان فرانت و بک‌اند و گارد واحد `isAdminRole`
- [x] بستن کنسول قدیمی `/admin` و ریدایرکت مسیرهای قدیمی `/users` و `/notify-settings` به تب‌های «تنظیمات»
- [x] حذف ساخت خودکار دادهٔ نمایشی هنگام bootstrap و نام‌گذاری واقعی سازمان
- [x] عضویت چندسازمانی: یکتایی نام کاربری در سطح تننت و ورود با انتخاب اجباری سازمان
- [x] تغییر سازمان بدون خروج از نشست: `GET /app-auth/organizations` + `POST /app-auth/switch-organization` و `OrganizationSwitcher` در هدر
- [x] اعتبارسنجی عملی E2E عضویت چندسازمانی روی API در حال اجرا (سناریو + نتایج در «شواهد تست چندسازمانی»)
- [x] حذف منوی «اقدامات» از ناوبری برای همهٔ نقش‌ها (بدون حذف داده/CRUD)
- [x] دستور جلسه با بندهای افزودنی/حذفی + پیوست فایل جلسه در باکت خصوصی
- [x] ارسال ایمیل دعوت شامل متن دستور جلسه و فایل‌های پیوست (MIME)
- [x] ویرایش بندها توسط مدیر/دبیر + دکمهٔ «ارسال دوبارهٔ دستور جلسه و پیوست‌ها» با ثبت Audit Log
- [x] صفحهٔ یکپارچهٔ «تنظیمات هوش مصنوعی» در سطح هر تننت (فقط مدیر) با کلید ماسک‌شده و تست اتصال واقعی
- [x] اولویت‌دهی و fallback زنجیره‌ای STT/LLM با ثبت تأمین‌کنندهٔ موفق/ناموفق
- [ ] رونویسی diarization با قطعه‌بندی زمان‌دار، جست‌وجو، دانلود زمان‌دار/بدون زمان و ویرایش متن‌ها
- [x] بک‌اند «دستیار هوشمند»: سرویس بازیابی محتوای جلسات + راهنمای سامانه و روتر `/api/v1/assistant` (status/ask)
- [x] RBAC دستیار: دسترسی فقط مدیر/دبیر و پاسخ ۴۰۳ فارسی برای عضو، همراه ثبت Audit و مصرف
- [x] پنل شناور/کناری دستیار در همهٔ صفحات با دو حالت، نمایش منابع و لینک بازگشت به جلسه
- [x] رفتار امن بدون مدل زبانی فعال: پاسخ راهنمای منابع واقعی، بدون خطای خام و بدون منبع ساختگی
- [x] اعمال پالت رنگ برند جدید در توکن‌های `index.css` و `tailwind.config.ts` (primary #2B7FFF، light #BDDBFE، سطح خاکستری #F3F4F6، خطا #FEF0F0/#F79283)
- [x] یکسان‌سازی نام «ویدارا - نسخه جلسات» در فرانت (ورود، پوسته، بلاگ، متادیتا) و بک‌اند (متن ایمیل/پیامک و تنظیمات اعلان)
- [x] جایگزینی لوگو و فاویکون با فایل‌های رسمی برند (لوگوی افقی در ورود/هدر، آیکون در فاویکون/apple-touch/og)
- [x] پاک‌سازی رنگ‌های ثابت باقی‌مانده در صفحات بلاگ، مقاله، نمای چاپ صورتجلسه و نشانگر بارگذاری
- [x] اعتبارسنجی نهایی: lint، build و بررسی رندر مرورگر
- [x] رفع قطعی عنوان/آیکون زبانهٔ مرورگر: حذف صفت‌های `data-mgx-overview`، ساخت `favicon.ico` چندسایز از آیکون رسمی، نسخه‌بندی کش (`?v=3`) و قفل زمان اجرا با `src/lib/brand.ts`
- [x] پایه‌های موبایل: `viewport-fit=cover`، جلوگیری از zoom خودکار iOS روی ورودی‌ها، حذف اسکرول افقی سراسری و ابزارک‌های ناحیهٔ لمسی/safe-area
- [x] منوی همبرگری موبایل: انتقال ناوبری، تغییر سازمان، تنظیمات مدیر، حساب کاربری و خروج به drawer با حفظ گاردهای نقش
- [x] هدر موبایل ساده (لوگو + همبرگری) و نمایش سهمیه/اعلان‌ها به‌شکل جمع‌وجور
- [x] پنل دستیار هوشمند در موبایل تمام‌صفحه و جابه‌جایی دکمهٔ شناور تا با منو تلاقی نکند
- [x] جدول‌های کاربران و گزارش ارسال‌ها در موبایل به نمای کارتی تبدیل شوند
- [x] نوارهای زبانه‌ای (تنظیمات، جزئیات جلسه، هوش مصنوعی، جلسات، ورود) در موبایل اسکرول افقی کنترل‌شده بگیرند
- [x] بازبینی صفحات ورود/داشبورد/جلسات/جزئیات جلسه/تنظیمات/حساب در عرض ۳۲۰ تا ۴۱۴ پیکسل
- [x] اعتبارسنجی: lint، build و بررسی رندر موبایل با باز/بسته کردن واقعی منوی همبرگری
- [x] تقویم شمسی در ثبت/ویرایش جلسه: انتخابگر جلالی (ماه فارسی، شروع هفته شنبه، تشخیص «امروز») + ساعت، و یکسان‌سازی نمایش تاریخ‌ها
- [x] نوار پیشرفت واقعی آپلود با XHR (درصد واقعی، لغو، وضعیت جداگانهٔ هر فایل) در ثبت جلسه، کارت پیوست‌ها و بارگذاری صوت
- [x] رفع باگ آپلود بی‌صدا: اعتبارسنجی پیش از ارسال (حجم/نوع)، خطای فارسی روشن، تلاش دوباره و پیام موفق/ناموفق تجمیعی
- [x] پذیرش نام فایل فارسی/کاراکترخاص: کلید امن در Storage + نگهداری نام اصلی و دانلود با نام فارسی درست
- [x] پیشنهاد هوشمند مصوبات و اقدامات از رونویسی در تب «مصوبات و اقدامات» با بازبینی/ویرایش/قبول تک‌تک و گارد نقش
- [x] دانلود Word صورتجلسه کنار «نمای چاپ»: docx واقعی RTL با سربرگ سازمان، تاریخ شمسی، حاضران/غایبان، مصوبات/اقدامات و امضاها
- [x] رفع باگ متغیر تعریف‌نشدهٔ `port` در `_execute_minutes` که کار پیش‌نویس هوشمند را می‌شکند
- [x] سقف‌های بارگذاری تنظیم‌پذیر سازمان: مدل/سرویس/endpoint و اتصال اعتبارسنجی صوت و پیوست به همان مقادیر
- [x] زبانهٔ «سقف‌های بارگذاری» در تنظیمات سازمان با خواندن/ذخیره/بازگردانی به پیش‌فرض و بازهٔ مجاز
- [x] جایگزینی صفحهٔ انتظار با گیف برند (کامپوننت مشترک `LoadingGif` در پوستهٔ برنامه و دیالوگ ثبت جلسه)
- [x] رفع «دیده‌نشدن نوار پیشرفت آپلود»: نوار پیشرفت و لغو برای صوت جلسه، و نمایش مرحله‌ای «در نوبت/در حال بارگذاری» برای پیوست‌های ثبت جلسه
- [x] آداپتور استوریج خارجی (S3 سازگار و WebDAV) با انتقال جریانی، چکسام SHA-256 و تست واقعی اتصال
- [x] مقصد ذخیره‌سازی در سطح سازمان با اعتبارنامهٔ رمزنگاری‌شده و پاسخ ماسک‌شده به فرانت
- [x] سرویس آرشیو/بازیابی فایل‌های جانبی جلسه با verify-before-delete و مهلت نگه‌داری نسخهٔ بازیابی‌شده
- [x] روتر امن `/api/v1/archive` فقط برای مدیر سازمان با صف Jobs، درصد پیشرفت، retry و ثبت audit
- [x] زبانهٔ «استوریج خارجی و آرشیو» در تنظیمات: فرم مقصد، تست اتصال، وضعیت فایل‌به‌فایل و دکمه‌های آرشیو/بازیابی
- [x] افزودن دبیر/عضو با حداقل اطلاعات (نام، نام خانوادگی، موبایل) + رمز دلخواه یا رمز پیش‌فرض `vidara@12345`
- [x] تکمیل اجباری مشخصات در نخستین ورود: نام کاربری جدید، رمز عبور جدید و کد ملی (`POST /app-auth/complete-profile` + صفحهٔ `/complete-profile` با گارد در ورود/پوسته/تغییر سازمان)
- [x] کد ملی به‌عنوان شناسهٔ اصلی هویت: سازگاری اجباری نام و نام خانوادگی با سوابق همان کد ملی در کل سامانه (ثبت‌نام/ساخت کاربر/تکمیل مشخصات/ویرایش)
- [x] ورود بدون مرحلهٔ انتخاب سازمان: جفت «نام کاربری + رمز عبور» مستقیماً کاربر را به همان فضای کاری می‌برد و جابه‌جایی بین فضاها مستلزم نام کاربری و رمز همان فضاست
- [x] حذف خودخدمتی سازمان با همهٔ داده‌ها (M14): `POST /app-auth/delete-organization` با تأیید عبارتی + نام دقیق سازمان و پنل «ناحیهٔ خطرناک» در تنظیمات
- [x] پوستهٔ دسکتاپ به سبک دیپ‌سیک: همهٔ منوها در سایدبار سمت راست جمع‌شونده (آیکونی/کامل با حفظ وضعیت در مرورگر) و در موبایل منوی همبرگری در سمت راست + drawer راست
- [x] ساده‌سازی منوی حساب: «حساب من» و «تغییر رمز عبور» در آیتم واحد «حساب کاربری» ادغام شدند؛ «مدیریت کاربران» و «تنظیمات ایمیل/پیامک» از منو حذف شدند (در زبانه‌های تنظیمات سازمان موجودند) و بخش پایینی سایدبار کاملاً حذف شد (سهمیه و خروج به نوار بالا رفتند)
- [x] تست E2E جامع همهٔ امکانات روی سرور تولید (۱۳۲ کیس، ۱۶ گزارش وضعیتی) + رفع باگ اعتبارسنجی مدت جلسه
- [x] استقرار کامل روی سرور تازهٔ 212.80.18.20 (Ubuntu 24.04): آینهٔ داکر آروان، انتقال کد با SCP، .env با پیش‌فرض «حرف»، نصب + تست E2E کامل ۱۳۲ کیس + رفع باگ healthcheck بدون curl

## استقرار روی سرور تازهٔ تولید (212.80.18.20 — 2026-08-26)
- سرور: Ubuntu 24.04، ۸ هسته، ۱۵GB رم، ۹۰GB دیسک آزاد. داکر 29.7.2 از قبل نصب بود؛ آینهٔ `docker.arvancloud.ir` در `daemon.json` تنظیم شد (داکرهاب مسدود است) و کاربر samim به گروه docker اضافه شد. nginx میزبان (صفحهٔ Hello World) متوقف/غیرفعال شد تا پورت ۸۰ آزاد شود.
- کد: چون گیتهاب از سرور مسدود است، آخرین نسخه با `git archive` + SCP منتقل و در `~/vidara-meetings` باز شد (شامل .wheels و bundle آفلاین بک‌اند).
- `.env`: دامنهٔ اصلی = دامنهٔ فایل‌ها = `vidara-meeting.ir` با `STORAGE_PORT=8443` (زیردامنهٔ storage در DNS وجود ندارد)؛ `STORAGE_PUBLIC_URL=http://vidara-meeting.ir:8443`؛ اعتبارنامهٔ پیش‌فرض «حرف» (samim2) فعال شد تا رونویسی سازمان‌های تازه بدون پیکربندی کار کند.
- باگ استقرار رفع‌شده: بررسی سلامت در install.sh/update.sh به `curl` وابسته بود که در ایمیج python:3.11-slim نیست → نصب با «بک‌اند سالم نشد» متوقف می‌شد؛ اکنون با خودِ پایتون انجام می‌شود. ایمپورت گم‌شدهٔ `db_manager` در main.py (بازیابی کارهای AI نیمه‌کاره) هم اصلاح شد. کامیت `286670e`.
- نتیجه: ۷ کانتینر سالم (db/minio/oss-gateway/backend/frontend/proxy/certbot)؛ تست E2E کامل ۱۳۲/۱۳۲ PASS؛ خط لولهٔ AI واقعی (آپلود مستقیم با URL امضاشده → فراخوانی «حرف» با اعتبارنامهٔ معتبر) کار می‌کند.
- **SSL مانده است:** ترافیک ورودی ۸۰/۴۴۳ از خارج کشور در لبهٔ دیتاسنتر بسته است (تأیید با check-host از ۵ کشور)؛ Let's Encrypt با HTTP-01 به همین دلیل timeout می‌شود. DNS دامنه روی ابر آروان است؛ راه‌حل‌ها: فعال‌کردن CDN ابر آروان (گواهی خودکار در لبه) یا تأیید DNS-01 با کلید API آروان، یا باز کردن ورودی خارجی در پنل دیتاسنتر. تا آن زمان سایت روی HTTP و فایل‌ها روی HTTP:8443 کار می‌کنند.
- به‌روزرسانی بعدی: تاربال جدید + `cd deploy && bash scripts/update.sh` (بدون وابستگی به گیت روی سرور).

## نتایج تست E2E جامع همهٔ امکانات (روی سرور تولید — 2026-08-25)
- **نتیجه: ۱۳۲ PASS / ۰ FAIL** + ۱۶ INFO (خروجی‌ها در `.deploy-tools/e2e-full.py` و لاگ run4).
- پوشش: سلامت/پیکربندی، ثبت‌نام/ورود/رمز/تکمیل مشخصات/تأیید ایمیل و موبایل/مدیریت کاربران (نقش، غیرفعال‌سازی، بازنشانی)، چندمستاجری، تنظیمات (AI با تست اتصال واقعی، SMTP، پیامک، سقف‌های بارگذاری با کلمپ، استوریج خارجی با ماسک کلید)، جلسات (ایجاد/ویرایش/حذف/فیلتر/جست‌وجو/دستور/بازچینی/پیوست)، دعوت/RSVP/حضور/دعوت‌نامه، صوت (URL امضاشده، رد پسوند/حجم، مهر رضایت، پخش، حذف)، کارهای AI (رونویسی، پیش‌نویس، پیشنهاد مصوبات، retry، چرخهٔ وضعیت)، صورتجلسه (نسخه‌ها، مصوبات، اقدامات، ارسال/رد/تأیید/قفل و گاردها)، خروجی JSON/Word/ICS، کنسول مدیر، اعلان‌ها، audit، داشبورد، دستیار هوشمند، جداسازی مستأجر (۴۰۴ همه‌جا) و حذف کامل سازمان (گارد عبارتی + نام).
- **باگ یافت‌شده و رفع‌شده:** مدت جلسه فقط به ۵ کلمپ می‌شد و سقف ۴۸۰ نداشت؛ اکنون در ایجاد و ویرایش، خارج از بازهٔ ۵..۴۸۰ با پیام صریح رد می‌شود (کامیت `0ff799b`).
- **مشکل زیرساختی یافت‌شده (نیازمند دسترسی SSH سرور):** TLS روی پورت‌های ۴۴۳ و ۸۴۴۳ سرور خراب است (`WRONG_VERSION_NUMBER`/`RECORD_LAYER_FAILURE`)؛ سایت فقط روی HTTP/80 کار می‌کند و چون URLهای امضاشدهٔ آپلود با `https://` صادر می‌شوند، **آپلود/دانلود مستقیم فایل برای کاربر واقعی هم شکست می‌خورد** (روی HTTP://8443 خط لولهٔ S3 سالم است). رونویسی هوشمند هم با خطای SSL سمت سرور به تأمین‌کننده می‌رسد (`harf: SSL WRONG_VERSION_NUMBER`) با آنکه تست اتصال اعتبارنامه (glogin) موفق است — به‌احتمال زیاد همان مشکل TLS/پراکسی شبکهٔ سرور است.

## Progress Log
- 2026-08-21 | Alex | بستهٔ استقرار مستقل ساخته شد: ایمیج بک‌اند (ffmpeg/فونت)، ایمیج چندمرحله‌ای فرانت + nginx، دروازهٔ ذخیره‌سازی روی MinIO با همان قرارداد ObjectStorage، compose با انتشار تنها ۸۰/۴۴۳ و volume ماندگار، reverse proxy دو دامنه با SSL و تمدید خودکار، `GET /api/config` برای نشانی پایهٔ API، اسکریپت‌های نصب/پشتیبان/بازیابی/به‌روزرسانی با گارد کلیدهای حیاتی، و مستند فارسی استقرار. اعتبارسنجی واقعی: `bash -n` ده اسکریپت (SH_OK)، صحت YAML و پورت/volume، `py_compile` (PY_COMPILE_OK)، تست روتر پیکربندی (RUNTIME_CONFIG_ROUTER_OK)، `pnpm run lint` و `pnpm run build` موفق. کد قابلیت‌ها تغییر نکرد.
- [Emma] تسک ۱ انجام شد: بازبینی دامنهٔ فیچرهای MVP در `docs/mvp_feature_review.md` (۲۳ فیچر ورودی ارزیابی شد؛ ۱ حذف، ۲ ادغام، ۲ انتقال به فاز بعد، ۵ کاهش دامنه، ۱۴ فیچر جدید Must/Should پیشنهاد شد).
- [Emma] اولویت‌بندی MoSCoW: Must ≈ ۹۰ روز-نفر، Should ≈ ۳۰، Could ≈ ۸ (+ ۵ روز آزمون غیرکارکردی)؛ توالی تحویل در ۴ برش پیشنهاد شد.
- [Emma] وابستگی‌ها اصلاح شد (۸ مورد) و ۱۴ معیار پذیرش ناقص به معیار کمّی قابل آزمون تبدیل شد؛ ۸ ابهام (Q1–Q8) با مقدار پیش‌فرض پیشنهادی ثبت شد.
- [Emma] الزامات غیرکارکردی ۱۰۰ کاربر همزمان به بار عددی قابل آزمون ترجمه شد؛ ورودی معماری برای Bob آماده است (بخش‌های ۵.۱، ۶.۱، ۶.۳).
- [Bob] تسک ۲ انجام شد: سند کامل معماری در `docs/architecture.md` (۱۶ بخش) شامل نمای اجزا، پشتهٔ فنی با دلیل انتخاب، ERD چندمستأجری، قراردادهای REST و ماتریس RBAC سه نقش، AI Gateway و صف، اعلان‌ها، Audit و پایش، امنیت صوت، کنسول پلتفرم، ظرفیت‌سنجی، استقرار و ۱۲ ADR.
- [Bob] چهار چالش بحرانی و پاسخ معماری آن‌ها تعیین شد: جداسازی مستأجر با اجبار دو لایه (Repository + RLS)، کارهای AI روی صف پایدار با DLQ داده‌ای، آپلود مستقیم به Storage با presigned multipart، و نرمال‌سازی متن فارسی برای جست‌وجو.
- [Bob] طرح ظرفیت ۱۰۰ کاربر همزمان: بودجهٔ تأخیر ۵۰۰ms، ۲ نمونهٔ API + ۲ Worker AI (۱۰ کار همزمان، ۳ در هر سازمان)، connection pool ۴۹ از ۱۰۰، و ۸ آزمون پذیرش ظرفیت به‌عنوان پیش‌نیاز عرضه.
- [Bob] `.atoms/ARCHITECTURE.md` با نمای سیستم، پشتهٔ فنی، طراحی ماژول‌ها، تصمیم‌های فنی، ساختار فایل و راهنمای پیاده‌سازی چهار برشی به‌روزرسانی شد.
- [Bob] تسک ۳ انجام شد: مستندات واقعی سرویس «حرف» (Roshan AI) در `uploads/harf` مطالعه و بخش‌های اثرپذیر سند معماری با قرارداد واقعی جایگزین شد (احراز هویت `/auth/glogin/`، endpoint `/api/transcribe_files/`، پارامترها و قالب پاسخ).
- [Bob] سه تفاوت مهم با فرض قبلی کشف و مستند شد: (۱) webhook وجود ندارد → الگوی رسمی `wait=false` + پایدارسازی `task_ids` + polling؛ (۲) `confidence` عددی وجود ندارد → ستون `confidence_avg` به `known_word_ratio` + `stats_words/stats_known_words` تغییر کرد؛ (۳) برچسب‌گذاری گوینده نیازمند نمونهٔ صدای از پیش ثبت‌شده است → انتساب گوینده از دامنهٔ MVP خارج شد.
- [Bob] نحوهٔ ارسال فایل از «URL امضاشده» به **multipart جریانی از MinIO** تغییر کرد تا Storage خصوصی بماند؛ `HARF_SEND_MODE=url` به‌عنوان گزینهٔ آگاهانهٔ عملیاتی باقی ماند.
- [Bob] محافظ هزینه اضافه شد: در retry، اگر `task_ids` موجود باشد فایل دوباره ارسال نمی‌شود و فقط polling ادامه می‌یابد؛ سهمیه بر پایهٔ `duration` بازگشتی سرویس (نه تخمین محلی) ثبت می‌شود و مجموع دقایق قطعات پیش از ارسال اولین قطعه بررسی می‌گردد.
- [Bob] ظرفیت اصلاح شد: سقف همزمانی **سه سطحی** (۱۰ کار سیستم / ۳ هر سازمان / ۴ درخواست همزمان به «حرف»)، متریک‌های `harf_*`، timeout پویا `max(۱۵ دقیقه, ۱٫۵× مدت صوت)`، و chunking با `ffmpeg` روی مرز سکوت برای فایل‌های بلند.
- [Bob] سه ADR جدید (۱۳ الگوی polling، ۱۴ سنجش کیفیت با `known_word_ratio`، ۱۵ خروج انتساب گوینده) و شش ابهام باز A8–A13 (سقف حجم/مدت، سقف نرخ، قیمت‌گذاری، سیاست نگه‌داری داده، پذیرش ویدیو، کدهای خطا) ثبت شد.
- [Alex] فرانت‌اند کامل شد: لایهٔ API هم‌راستا با روترهای `/api/v1/...`، پوستهٔ RTL با ناوبری/اعلان/سهمیه، و هفت صفحهٔ معرفی، داشبورد، جلسات، جزئیات جلسه، اقدامات، کنسول مدیریت و نمای چاپ.
- [Alex] جریان end-to-end وصل شد: ثبت جلسه و دعوت، RSVP و حضور، بارگذاری صوت با URL امضاشده و تأیید رضایت، رونویسی غیرهمزمان با polling و retry، پیش‌نویس هوشمند صورتجلسه، گردش پیش‌نویس→تأیید→قفل با نسخه‌بندی، مصوبات و اقدامات، خروجی ICS و چاپ PDF فارسی.
- [Alex] اعتبارسنجی: `py_compile` روی پنج فایل بک‌اند بدون خطا، `pnpm run lint` بدون خطا، `pnpm run build` موفق، و بررسی رندر UI با نتیجهٔ قابل قبول.
- [Alex] بازطراحی UI/UX با الهام از مرجع مدرن SaaS: سیستم توکن رنگ/تم و تایپوگرافی در `index.css` بازنویسی شد؛ لندینگ با قاب اول سینمایی، ویژگی‌ها، نحوهٔ کار، اعتماد، پلن‌ها و CTA بازسازی شد؛ هدر چسبان و برندینگ پوستهٔ اپ بهبود یافت. منطق بک‌اند و قراردادهای API دست‌نخورده ماند.
- [Alex] تضمین کار با دادهٔ واقعی: endpoint `POST /api/v1/workspace/demo-data/purge` در `routers/workspace.py` افزوده شد (حذف جلسات/صورتجلسه/مصوبات/اقدامات/ترنسکریپت/ریکوردینگ/jobs/دعوت‌ها/اعضای نمایشی)، wrapper `api.purgeDemoData` در `lib/mgmt.ts` و پنل `DemoDataPanel` با تأیید عبارتی «پاک‌سازی» در کنسول مدیریت اضافه شد؛ همهٔ CRUDها روی دادهٔ واقعی سازمان اجرا می‌شوند.
- [Alex] اعتبارسنجی نهایی این دور: `python -m py_compile routers/workspace.py` بدون خطا، `pnpm run lint` بدون خطا، `pnpm run build` موفق (prerender صفحهٔ اصلی و بلاگ)، و بررسی رندر UI با نتیجهٔ عالی.
- [Alex] برندینگ سفارشی جایگزین شد: کامپوننت `components/VidaraBranding.tsx` با لوگوی کاربر در `public/assets/vidara-logo.svg` و عبارت «توسعه داده شده توسط تیم ویدارا. نسخه آزمایشی» در فوتر لندینگ، فوتر فضای کاری و پای صفحهٔ چاپ/PDF درج شد؛ `index.html` (title/favicon/og/twitter)، `vite.config.ts` (عنوان، توضیح، آدرس لوگو، hostname نقشهٔ سایت) و `lib/blog.ts` (نام سایت و هندل توییتر) از برندینگ پیشین پاک شد و جست‌وجوی سراسری در `src` و `public` هیچ اثری از آن نشان نداد.
- [Alex] احراز هویت مستقل کامل شد: جدول `app_users`، سرویس `services/app_auth.py`، dependency هدر `X-App-Token` و روتر `routers/app_auth.py` (register/login/me/change-password/users) ساخته شد و همهٔ روترهای دامنه (`workspace`, `minutes_flow`, `meeting_ai`, `storage`) به `get_workspace_user` منتقل شدند تا مرز مستأجر روی نشست مستقل اعمال شود.
- [Alex] سیستم اعلان جلسه پیاده شد: `models/org_notify_settings.py` و `models/notify_deliveries.py` برای تنظیمات و لاگ، `services/notify_channels.py` برای SMTP و پیامک قاصدک با متن فارسی/تاریخ شمسی و پیوست ICS، `services/meeting_invites.py` برای ارسال دعوت و ثبت نتیجه، و `routers/notify_settings.py` برای خواندن/ویرایش تنظیمات، تست ارسال ایمیل/پیامک، گزارش ارسال‌ها و ارسال دوباره.
- [Alex] ایجاد جلسه به اعلان وصل شد: در `routers/workspace.py` پس از commit، `send_meeting_invites` برای همهٔ شرکت‌کنندگان اجرا می‌شود و نتیجهٔ هر ارسال در `notify_deliveries` ثبت می‌گردد.
- [Alex] لندینگ و ریشهٔ `/` کاملاً حذف شد: `pages/Index.tsx`، `pages/AuthCallback.tsx`، `pages/AuthError.tsx` و `pages/LogoutCallbackPage.tsx` پاک شدند، `App.tsx` بازنویسی شد تا `/` به صفحهٔ ورود مستقل برسد و مسیرهای `/users`, `/notify-settings`, `/account` اضافه شوند.
- [Alex] پوستهٔ برنامه بازنویسی شد: `components/AppShell.tsx` نشست را از توکن مستقل می‌خواند، در نبود نشست به `/` هدایت می‌کند، ناوبری را بر پایهٔ نقش (admin ⇒ کاربران/تنظیمات اعلان/مدیریت سازمان) می‌سازد و دکمهٔ خروج نشست را پاک می‌کند.
- [Alex] فرانت‌اند اعلان و کاربران: `lib/session.ts` و `lib/appAuth.ts` و `lib/notify.ts` به‌همراه صفحات `Login.tsx`، `Account.tsx`، `Users.tsx` و `NotifySettings.tsx` (ویرایش SMTP/قاصدک، تست ارسال، گزارش ارسال‌ها) تکمیل شد.
- [Alex] نام محصول یکدست شد: عنوان پیش‌فرض در `vite.config.ts`، متادیتای `index.html` و نام سایت در `lib/blog.ts` به «ویدارا — نسخه جلسات» تغییر یافت.
- [Alex] اعتبارسنجی این دور: `pnpm run lint` بدون خطا و `pnpm run build` موفق (خطای آیکن ناموجود `MailCog` شناسایی و با آیکن `Mail` جایگزین شد).
- [Alex] رفع باگ ۴۰۹ ثبت‌نام: علت اصلی این بود که ثبت‌نام قبلی در دیتابیس موفق شده بود و تلاش دوباره با همان موبایل/نام کاربری به تعارض می‌خورد بدون هیچ راه خروج. اکنون `find_existing_account` + `mobile_variants` در `services/app_auth.py` یکتایی را روی نام کاربری و همهٔ نگارش‌های شماره موبایل (۰۹…/۹…/+۹۸/۰۰۹۸) بررسی می‌کند، `duplicate_account_message` پیام راهنما با نام کاربری واقعی و ارجاع به «ورود» می‌دهد، ورود با `find_login_user` هم با نام کاربری و هم با شمارهٔ موبایل کار می‌کند، و در `pages/Login.tsx` هنگام خطای ۴۰۹ دکمهٔ «رفتن به صفحهٔ ورود با همین حساب» با پیش‌پر کردن شناسه نمایش داده می‌شود. اعتبارسنجی: `py_compile` سه فایل بک‌اند، `pnpm run lint` و `pnpm run build` موفق.
- [Alex] رفع باگ ۵۰۰ روی `GET /api/v1/workspace/bootstrap`: در `dependencies/app_auth.py` فیلد نادرست `principal.full_name` به `principal.name` تغییر یافت (کلاس `AppPrincipal` فیلد `name` دارد)؛ جست‌وجوی سراسری نشان داد سایر `\.full_name`ها روی رکورد `Memberships` هستند و درست‌اند و زنجیرهٔ `create_app_user` → عضویت با شناسهٔ `app:<id>` → `resolve_context` سازگار است؛ `python -m py_compile` روی پنج فایل بک‌اند بدون خطا.
- [Alex] بهبود کشف‌پذیری مسیرهای مدیر: کامپوننت جدید `components/AdminQuickStart.tsx` وضعیت واقعی راه‌اندازی را از `authApi.listUsers` و `notifyApi.readSettings` می‌خواند و برای هر گام ناقص (افزودن کاربران، تنظیم SMTP، تنظیم پنل پیامک قاصدک) کارت اقدام قابل کلیک به `/users` و `/notify-settings` نشان می‌دهد؛ این بخش فقط برای نقش مدیر در `pages/Dashboard.tsx` رندر می‌شود.
- [Alex] ناوبری و منوی حساب: برچسب‌های `ADMIN_NAV` در `components/AppShell.tsx` به «مدیریت کاربران» و «تنظیمات ارسال ایمیل و پیامک» تغییر یافت و منوی بازشوی حساب در هدر با میان‌بر «حساب من»، «تغییر رمز عبور» و (برای مدیر) «مدیریت کاربران و نقش‌ها» و «تنظیمات ارسال ایمیل و پیامک» افزوده شد.
- [Alex] شفاف‌سازی اعتبارنامهٔ کاربر جدید: در `pages/Users.tsx` عنوان و توضیح صفحه اضافه شد و نمایش اعتبارنامه با قرارداد واقعی روتر هم‌راستا گردید (`default_credentials.username`/`password_hint`) تا نام کاربری = موبایل و رمز اولیه = کد ملی به مدیر نمایش داده شود؛ نوع `CreatedUserResult` در `lib/appAuth.ts` نیز اصلاح شد.
- [Alex] اعتبارسنجی این دور: `pnpm run lint` بدون خطا، `pnpm run build` موفق و بررسی رندر UI با نتیجهٔ قابل قبول.
- [Alex] پیوست دستور جلسه کامل شد: `models/meeting_attachments.py`، `services/meeting_files.py` (باکت خصوصی `meeting-attachments`، سقف ۲۵MB، انواع مجاز، URL امضاشدهٔ آپلود/دانلود) و روتر `routers/meeting_attachments.py` با فهرست/آپلود/ثبت/دانلود/حذف و کنترل `require_meeting_manager` و ثبت Audit برای هر عملیات مؤثر.
- [Alex] ایمیل دعوت غنی شد: `services/notify_channels.py` از `EmailAttachment` و MIME چندبخشی پشتیبانی می‌کند و `services/meeting_invites.py` بندهای دستور جلسه و فایل‌های پیوست را به متن و ضمیمهٔ ایمیل اضافه می‌کند؛ فایل‌های بزرگ‌تر از ۸MB ضمیمه نمی‌شوند و فقط نامشان در متن ذکر می‌گردد.
- [Alex] «ارسال دوبارهٔ دستور جلسه و پیوست‌ها» با `POST /meetings/{id}/resend-agenda` پیاده شد: ابتدا Audit با کنش `meeting.agenda.resent` ثبت و سپس ارسال انجام می‌شود و نتیجهٔ هر گیرنده در `notify_deliveries` لاگ می‌گردد.
- [Alex] فرانت هم‌خوان شد: `components/MeetingAttachmentsCard.tsx` در زبانهٔ «دستور جلسه و حضور» با بارگذاری، فهرست، دانلود امضاشده، حذف و دکمهٔ ارسال دوباره؛ متدهای پیوست و `uploadMeetingAttachment`/`formatFileSize` به `lib/mgmt.ts` افزوده شد.
- [Alex] منوی «اقدامات» از ناوبری حذف شد و مسیر `/actions` به `/meetings` هدایت می‌شود؛ لینک داشبورد به «همهٔ جلسات» تغییر یافت (داده و CRUD اقدامات دست‌نخورده در زبانهٔ «مصوبات و اقدامات» باقی است).
- [Alex] اعتبارسنجی این دور: `python -m py_compile` روی چهار فایل بک‌اند بدون خطا، `pnpm run lint` بدون خطا و `pnpm run build` موفق.
- [Alex] ورود چندسازمانی در UI تکمیل شد: `lib/appAuth.ts` نوع `LoginOrganizationOption`/`LoginResult` و helper `needsOrganizationChoice` گرفت و `authApi.login` پارامتر سوم `organizationId` را می‌فرستد؛ در `pages/Login.tsx` وقتی بک‌اند `needs_organization` برگرداند، فهرست سازمان‌ها با نقش هر کدام در یک Select نمایش داده می‌شود و ورود با شناسهٔ سازمان انتخابی تکمیل می‌گردد.
- [Alex] فرم «تعریف جلسه جدید» کامل شد: `MeetingIn` در `routers/workspace.py` فیلد `agenda_items` (مدل `AgendaItemIn`) گرفت و بندها پیش از ارسال دعوت‌نامه در `Agenda_items` ثبت می‌شوند؛ در `pages/Meetings.tsx` بخش افزودن/حذف بند (عنوان، زمان، مسئول، توضیح) و انتخاب چندفایلی پیوست اضافه شد که پس از ساخت جلسه با `uploadMeetingAttachment` بارگذاری می‌شود.
- [Alex] زبانهٔ «هوش مصنوعی» در تنظیمات سازمان اضافه شد: کلاینت `lib/aiSettings.ts` و پنل `components/settings/AiProvidersPanel.tsx` با سه زبانهٔ فرعی رونویسی گفتار، مدل زبانی و زنجیرهٔ اولویت/جانشینی؛ هر تأمین‌کننده نشانی سرویس، نام مدل، اعتبارنامهٔ ماسک‌شده، تفکیک گوینده، فعال/غیرفعال و «تست اتصال» واقعی دارد و ترتیب اولویت با دکمه‌های بالا/پایین جابه‌جا می‌شود.
- [Alex] نمای زنجیرهٔ اولویت/fallback ساخته شد: ترتیب واقعی اجرای بک‌اند از `GET /api/v1/ai-settings/chain` خوانده و همراه جانشین پیش‌فرض پلتفرم نمایش داده می‌شود؛ نتیجهٔ کار صورتجلسه در `routers/meeting_ai.py` نیز تأمین‌کنندهٔ موفق و شرح تلاش‌ها را ثبت می‌کند.
- [Alex] اعتبارسنجی این دور: `python -m py_compile` روی `routers/workspace.py` و `routers/ai_settings.py` بدون خطا، `pnpm run lint` بدون خطا، `pnpm run build` موفق و بررسی رندر UI نسخهٔ در حال اجرا با نتیجهٔ قابل قبول.
- [Alex] تغییر سازمان بدون خروج پیاده شد: `find_sibling_accounts` در `services/app_auth.py`، دو endpoint `GET /api/v1/app-auth/organizations` و `POST /api/v1/app-auth/switch-organization` در `routers/app_auth.py` (صدور توکن جدید با نقش سازمان مقصد) و کامپوننت `components/OrganizationSwitcher.tsx` در هدر `AppShell` که پس از تغییر، bootstrap را ریست و به `/dashboard` هدایت می‌کند تا نقش و منوها فوری به‌روز شوند.
- [Alex] باگ بحرانی ورود چندسازمانی با تست عملی کشف و رفع شد: منطق `/login` فهرست سازمان‌ها را فقط از حساب‌هایی می‌ساخت که رمزشان با ورودی یکی بود، ولی رمز هر سازمان مستقل است (سازمان دوم رمز اولیه = کد ملی دارد)؛ در نتیجه شخص عضو دو سازمان هم `needs_organization=false` می‌گرفت و همیشه به سازمان اول می‌رفت. اکنون فهرست از همهٔ حساب‌های **فعال** ساخته می‌شود، افشای فهرست مشروط به تطبیق رمز دست‌کم یک حساب است و ورود به سازمان انتخابی با رمز همان سازمان تأیید می‌شود.

- [Alex] پالت رنگ برند در لایهٔ توکن اعمال شد: در `src/index.css` رنگ اصلی `#2B7FFF`، تُن روشن `#BDDBFE`، سطح خاکستری `#F3F4F6` و رنگ‌های خطا (`#FEF0F0` پس‌زمینه + `#F79283` تأکید) تعریف و برای حالت تیره هم تنظیم شد؛ در `tailwind.config.ts` دو namespace رنگی `error` و `brand` اضافه گردید تا هیچ کامپوننتی نیازی به hex دستی نداشته باشد.
- [Alex] نام محصول در کل سامانه به «ویدارا - نسخه جلسات» یکسان شد: فرانت (`pages/Login.tsx`، `components/AppShell.tsx`، `lib/blog.ts`، `index.html`، `vite.config.ts`) و بک‌اند (`services/notify_channels.py` قالب ایمیل/پیامک، `routers/notify_settings.py` موضوع و متن نمونه، `services/app_auth.py`).
- [Alex] لوگو و آیکون رسمی برند جایگزین شد: لوگوی افقی در صفحهٔ ورود، آیکون برند در هدر پوسته و فوتر `components/VidaraBranding.tsx`، و در `index.html` فاویکون/apple-touch-icon/og-image به آیکون جدید تغییر یافت و `theme-color` برند اضافه شد؛ جست‌وجوی سراسری هیچ ارجاعی به `vidara-logo.svg` یا `favicon.svg` قدیمی باقی نگذاشت.
- [Alex] رنگ‌های ثابت باقی‌مانده پاک‌سازی شد: `pages/blog/BlogIndexPage.tsx`، `components/blog/BlogArticleLayout.tsx`، `components/blog/MarkdownArticle.tsx` (کلاس‌های `prose`)، `pages/PrintMinutes.tsx` (که فوتر آن به کامپوننت مشترک برند وصل شد) و `components/LoadingSpinner.tsx` همه به توکن‌های تم منتقل شدند.
- [Alex] اعتبارسنجی این دور: `pnpm run lint` بدون خطا، `pnpm run build` موفق (prerender صفحهٔ اصلی و بلاگ)، `python -m py_compile` روی `services/notify_channels.py`، `services/app_auth.py` و `routers/notify_settings.py` بدون خطا، و بررسی رندر مرورگر صفحهٔ ورود با پالت جدید و نتیجهٔ عالی.
- [Alex] ریشهٔ بازگشت عنوان/آیکون قدیمی در زبانهٔ مرورگر پیدا و رفع شد: صفت‌های `data-mgx-overview` روی `title`/`meta`/`link` باعث می‌شد لایهٔ میزبان مقادیر قدیمی را بازنویسی کند؛ این صفت‌ها از `index.html` حذف شد، `public/favicon.ico` چندسایز (۱۶/۳۲/۴۸/۶۴) و `public/assets/vidara-icon.png` از فایل رسمی برند بازتولید شد، همهٔ لینک‌های آیکون با پارامتر `?v=3` نسخه‌بندی شد تا کش مرورگر بشکند، و ماژول `src/lib/brand.ts` با MutationObserver عنوان و فاویکون را در زمان اجرا تثبیت می‌کند (فراخوانی در `src/main.tsx`، با استثنای صفحات prerender بلاگ).
- [Alex] شواهد واقعی این رفع باگ: جست‌وجوی سراسری در `app/frontend` هیچ ارجاعی به نام قدیمی برنگرداند؛ `dist/index.html` عنوان «ویدارا - نسخه جلسات» و چهار لینک آیکون `?v=3` دارد و هیچ `data-mgx-overview` روی تگ‌ها نیست؛ `dist/favicon.ico` و `dist/assets/vidara-icon.png` تولید شدند؛ پاسخ سرور در حال اجرا عنوان درست و `favicon.ico?v=3` با کد ۲۰۰ برگرداند؛ `pnpm run lint` بدون خطا، `pnpm run build` موفق و بررسی رندر مرورگر صفحهٔ ورود با نتیجهٔ خوب.
- [Alex] بهینه‌سازی موبایل کامل شد: پایه‌ها در `index.html` (`viewport-fit=cover`) و `src/index.css` (حذف اسکرول افقی، مهار `text-size-adjust` و اندازهٔ ورودی برای جلوگیری از zoom خودکار iOS)؛ هدر در موبایل به «لوگو + اعلان + همبرگری» ساده شد و کل ناوبری، تغییر سازمان، تنظیمات مدیر، حساب کاربری و خروج به drawer راست‌به‌چپ (`components/ui/sheet.tsx`) در `components/AppShell.tsx` منتقل شد، با حفظ کامل گاردهای نقش.
- [Alex] برای جلوگیری از دیالوگ تودرتو داخل drawer، `components/OrganizationSwitcher.tsx` حالت کنترل‌شده (`open`/`onOpenChange`/`hideTrigger`) گرفت تا از آیتم منو باز شود.
- [Alex] `components/AssistantPanel.tsx` در موبایل تمام‌صفحه (`fixed inset-0`) شد و اندازهٔ دکمه‌ها به آستانهٔ لمسی رسید تا با منوی همبرگری تلاقی نکند.
- [Alex] چیدمان صفحات برای عرض‌های ۳۲۰/۳۷۵/۴۱۴ اصلاح شد: جدول کاربران (`components/settings/UsersPanel.tsx`) و گزارش ارسال‌ها (`components/settings/NotifyPanels.tsx`) در موبایل نمای کارتی و در دسکتاپ جدول دارند؛ نوارهای زبانه در `pages/Settings.tsx`، `components/settings/AiProvidersPanel.tsx`، `pages/MeetingDetail.tsx` و `pages/Meetings.tsx` به اسکرول افقی کنترل‌شده و بدون شکست تغییر کردند؛ در `pages/Meetings.tsx` دکمهٔ «جلسهٔ جدید» و جست‌وجو تمام‌عرض شدند؛ در `pages/MeetingDetail.tsx` عنوان بلند شکسته می‌شود و سه دکمهٔ عملیات در موبایل تمام‌عرض و لمس‌پذیر شدند؛ در `pages/Dashboard.tsx` دکمهٔ سرصفحه تمام‌عرض و ارتفاع نمودارها برای صفحهٔ کوچک کاهش یافت؛ در `pages/Login.tsx` حاشیهٔ کارت کم و زبانه‌های ورود/ثبت‌نام به ارتفاع لمس‌پذیر رسید.
- [Alex] اعتبارسنجی این دور: `pnpm run lint` بدون خطا، `pnpm run build` موفق (prerender صفحهٔ اصلی و بلاگ) و بررسی رندر مرورگر در عرض موبایل با نتیجهٔ خوب (بدون اسکرول افقی و بدون شکستگی چیدمان).
- [Alex] بازماندهٔ صریح این دور: زبانهٔ diarization (قطعه‌بندی زمان‌دار، جست‌وجو، دانلود زمان‌دار/بدون زمان و ویرایش متن) هنوز پیاده نشده و تنها آیتم باز فهرست تسک‌هاست؛ رنگ‌آمیزی آن صفحه پس از ساخت باید با همین توکن‌ها انجام شود.

- [Alex] این دور کامل شد: (۱) رفع باگ متغیر تعریف‌نشدهٔ `port` در `routers/meeting_ai.py`؛ (۲) خروجی Word واقعی RTL با `services/minutes_docx.py` و endpoint `GET /api/v1/minutes-flow/export/{meeting_id}/docx` و دکمهٔ «دانلود فایل Word» کنار «نمای چاپ» در `pages/MeetingDetail.tsx`؛ (۳) پذیرش نام فایل فارسی با کلید امن Storage در `services/meeting_files.py` (حفظ پسوند) و دانلود با نام فارسی از `Content-Disposition` در `lib/mgmt.ts` (`saveBlob`, `downloadAttachment`, `downloadMinutesDocx`)؛ (۴) آپلود چندفایلی با درصد واقعی XHR، لغو و وضعیت جداگانهٔ هر فایل در `pages/Meetings.tsx` و `components/MeetingAttachmentsCard.tsx`؛ (۵) اتصال کامل UI پیشنهاد هوشمند مصوبات/اقدامات با ویرایش پیش از ثبت و پذیرش تک‌تک (`suggestMeetingItems` + فیلد `source` در `DecisionIn`/`ActionIn` در `routers/minutes_flow.py`)؛ (۶) یکسان‌سازی تاریخ شمسی با `JalaliDateTimePicker` برای مهلت اقدامات و `formatDate`/`formatDateTime` جلالی در سراسر جریان.
- [Alex] اعتبارسنجی این دور: `pnpm run lint` بدون خطا، `pnpm run build` موفق (prerender صفحهٔ اصلی و بلاگ)، `python -m py_compile` روی `routers/minutes_flow.py`، `routers/meeting_ai.py`، `routers/meeting_attachments.py`، `services/meeting_files.py` و `services/minutes_docx.py` بدون خطا، و بررسی رندر مرورگر روی سرویس در حال اجرا با نتیجهٔ خوب.

- [Alex] ریشهٔ «دیده‌نشدن نوار پیشرفت» در ثبت جلسه پیدا شد: نوار فقط پس از رسیدن اولین رویداد `progress` رندر می‌شد، در حالی که بازهٔ «ساخت جلسه + ارسال دعوت‌نامه» پیش از شروع آپلود طول می‌کشد و فایل‌های کوچک هم در یک پرش به ۱۰۰٪ می‌رسند. اکنون در `pages/Meetings.tsx` مرحلهٔ جاری (`creating`/`uploading`) تفکیک شده، همهٔ فایل‌ها از ابتدا با درصد صفر و برچسب «در نوبت بارگذاری» نمایش داده می‌شوند و نمایشگر گیف مرحله را اعلام می‌کند.
- [Alex] آپلود صوت جلسه در `pages/MeetingDetail.tsx` نوار پیشرفت واقعی + دکمهٔ لغو (`AbortController`) گرفت و سقف مدت/حجم مجاز از `getUploadLimits()` کنار فرم نمایش داده می‌شود.
- [Alex] صفحهٔ انتظار با گیف برند جایگزین شد: کامپوننت مشترک `components/LoadingGif.tsx` روی `public/assets/loader.gif` (کپی از فایل ارائه‌شدهٔ کاربر) در `components/AppShell.tsx` و دیالوگ ثبت جلسه استفاده می‌شود و اسکلت‌های انتظار قبلی برداشته شد.
- [Alex] سقف‌های بارگذاری تنظیم‌پذیر شد: `models/org_upload_limits.py`، `services/upload_limits.py` (پیش‌فرض/بازهٔ امن/snapshot)، endpointهای `GET|PATCH /api/v1/workspace/upload-limits` با گارد مدیر و ثبت Audit، و اتصال اعتبارسنجی در `routers/meeting_ai.py` و `routers/meeting_attachments.py`.
- [Alex] فرانت با سرور همسان شد: `lib/mgmt.ts` نوع `UploadLimits` و `applyUploadLimits`/`getUploadLimits` گرفت، سقف‌ها پس از bootstrap تزریق می‌شوند و پنل جدید `components/settings/UploadLimitsPanel.tsx` در زبانهٔ «سقف‌های بارگذاری» صفحهٔ تنظیمات، مقادیر را می‌خواند، اعتبارسنجی می‌کند، PATCH می‌زند و بی‌درنگ اعمال می‌کند.
- [Alex] اعتبارسنجی این دور: `pnpm i` بدون تغییر lockfile، `pnpm run lint` بدون خطا و `pnpm run build` موفق (prerender صفحهٔ اصلی و بلاگ).

- [Alex] استوریج خارجی و آرشیو جلسات کامل شد: `services/external_storage.py` (آداپتور S3 سازگار + WebDAV با آپلود/دانلود جریانی، محاسبهٔ SHA-256 هنگام انتقال، بررسی وجود شیء و تست واقعی اتصال)، `services/storage_targets.py` (اعتبارسنجی و ساخت `TargetConfig` سازمان)، `models/org_storage_targets.py` (اعتبارنامهٔ رمزنگاری‌شده با `encrypt_secret`/`decrypt_secret`) و `models/meeting_archive_files.py` (وضعیت، checksum، مسیر مقصد و خطای هر فایل).
- [Alex] سرویس `services/meeting_archive.py` جریان آرشیو/بازیابی فایل‌های جانبی (صوت جلسه + پیوست‌ها) را با ترتیب امن اجرا می‌کند: انتقال به مقصد ← تأیید وجود و تطبیق checksum ← تنها در صورت تأیید، حذف نسخهٔ داخلی؛ در بازیابی، فایل به باکت داخلی برمی‌گردد و مهلت نگه‌داری نسخهٔ بازیابی‌شده ثبت می‌شود.
- [Alex] روتر `routers/archive.py` فقط با گارد مدیر سازمان و مرز مستأجر منتشر شد: خواندن/ذخیرهٔ مقصد با پاسخ ماسک‌شده، تست اتصال، فهرست جلسات آرشیو، وضعیت فایل‌به‌فایل هر جلسه، صف کردن کار آرشیو/بازیابی روی جدول `jobs` با درصد پیشرفت، `retry` بدون تکرار مرحله‌های موفق و ثبت Audit برای هر کنش مؤثر.
- [Alex] فرانت وصل شد: قراردادها و توابع `archiveTarget`/`saveArchiveTarget`/`testArchiveTarget`/`archiveMeetings`/`archiveMeetingState`/`startMeetingArchive`/`startMeetingRestore`/`archiveJob`/`retryArchiveJob` به `lib/mgmt.ts` افزوده شد و پنل `components/settings/ExternalStoragePanel.tsx` (فرم مقصد S3/WebDAV، تست اتصال، نمایش وضعیت و خطای هر فایل، نوار پیشرفت job و دکمه‌های آرشیو/بازیابی/تلاش دوباره) در زبانهٔ جدید `archive` صفحهٔ `pages/Settings.tsx` رندر می‌شود.
- [Alex] اعتبارسنجی این دور: `pnpm run lint` بدون خطا و `pnpm run build` موفق (۲۷۸۹ ماژول، prerender صفحهٔ اصلی و بلاگ).

## شواهد تست چندسازمانی (E2E روی API در حال اجرا، ۱۴۰۵/۰۵/۲۹ — 2026-08-20)
- سناریو: شخص «رضا کاظمی» (موبایل `09121110099`، کد ملی `1122334499`) در «شرکت الف» مدیر سازمان و در «موسسه ب» عضو است (سازمان ب توسط مدیر دیگری ساخته و این شخص با نقش `member` افزوده شد).
- ورود بدون انتخاب سازمان → `200` با `needs_organization=true`، **بدون توکن**، و فهرست: `6 شرکت الف — مدیر سازمان` و `7 موسسه ب — عضو`.
- ورود با `organization_id=6` و رمز سازمان الف → `200`، سازمان فعال «شرکت الف»، نقش `org_admin` («مدیر سازمان») ⇒ آیکون/بخش تنظیمات در دسترس است.
- ورود با `organization_id=7` و رمز سازمان الف → `401` با پیام «رمز عبور شما در سازمان انتخاب‌شده متفاوت است…» ⇒ مرز مستأجر و استقلال رمز هر سازمان حفظ می‌شود.
- ورود با `organization_id=7` و رمز همان سازمان (کد ملی) → `200`، سازمان فعال «موسسه ب»، نقش `member` («عضو») ⇒ تنظیمات مخفی می‌ماند.
- `GET /me` در دو نشست موازی: نشست الف → `org_admin` / «شرکت الف»؛ نشست ب → `member` / «موسسه ب» ⇒ نقش هر نشست به سازمان فعال همان نشست چسبیده و آلوده نمی‌شود.
- `GET /app-auth/organizations` در نشست الف → دو آیتم با `is_current=true` برای سازمان ۶ و `current_organization_id=6`.
- `POST /app-auth/switch-organization` (الف → ب) → `200` با توکن جدید و نقش «عضو»؛ `GET /me` با توکن جدید هم «موسسه ب» / `member` را برمی‌گرداند ⇒ تغییر سازمان بدون خروج کامل و با تغییر فوری نقش کار می‌کند.
- اعتبارسنجی کد این دور: `python -m py_compile routers/app_auth.py` بدون خطا، `pnpm run lint` بدون خطا و `pnpm run build` موفق.

## شواهد تست «دستیار هوشمند» (E2E روی API در حال اجرا، 2026-08-20)
- دادهٔ واقعی ساخته شد: جلسهٔ «کمیتهٔ بودجه — بازبینی سقف هزینهٔ تجهیزات» (شناسه ۸) با دو بند دستور جلسه، صورتجلسهٔ متنی (شناسه ۳) و یک مصوبهٔ سقف خرید (شناسه ۴).
- حالت جست‌وجوی محتوا، پرسش «سقف خرید تجهیزات چقدر تعیین شد؟» → `200` با ۷ منبع واقعی؛ بالاترین امتیاز = مصوبهٔ «تعیین سقف خرید تجهیزات نیمهٔ دوم سال» با لینک بازگشت `/meetings/8`.
- حالت جست‌وجوی محتوا، پرسش «علت انحراف بودجه چه بود؟» → `200` با منابع «متن صورتجلسه» و «جمع‌بندی صورتجلسه» ⇒ بازیابی از بدنهٔ واقعی صورتجلسه کار می‌کند، نه فقط عنوان.
- پرسش کاملاً بی‌ربط «قرارداد ساخت نیروگاه هسته‌ای کجاست؟» → پیش از اصلاح ۵ منبع بی‌ربط برمی‌گشت؛ با افزودن آستانهٔ امتیاز مطلق (۱٫۵) و نسبی (۰٫۴۵) در `rank_chunks` و گسترش فهرست ایست‌واژه، به ۱ منبع کاهش یافت و منبع ساختگی تولید نمی‌شود.
- حالت راهنمای سامانه: «چطور صورتجلسه را تأیید و قفل کنم؟» → منبع «صورتجلسه: پیش‌نویس هوشمند، تأیید و قفل» با لینک `/meetings`؛ «تنظیم مدل زبانی سازمان از کجاست؟» → «تنظیمات هوش مصنوعی سازمان، اولویت و جانشینی» با لینک `/settings?tab=ai`.
- بدون مدل زبانی فعال: `model_available=false` و پاسخ متنی راهنمای منابع واقعی برمی‌گردد (بدون خطای خام ۵۰۰ و بدون ادعای جعلی).
- اعتبارسنجی ورودی: پرسش تک‌نویسه → `400` با پیام «پرسش خود را کمی کامل‌تر بنویسید (دست‌کم سه نویسه).»
- RBAC: نقش عضو روی `POST /assistant/ask` → `403` با پیام فارسی؛ مدیر و دبیر → `200`. پنل شناور در `AppShell` فقط برای مدیر/دبیر رندر می‌شود.
- Audit: رکوردهای `assistant.ask` در `audit_logs` ثبت شد (در کنار `minutes.saved` و `decision.created`).
- اعتبارسنجی کد این دور: `python -m py_compile services/assistant.py routers/assistant.py` بدون خطا، `pnpm run lint` بدون خطا، `pnpm run build` موفق و بررسی رندر مرورگر روی سرویس در حال اجرا (پورت ۳۰۰۰) با نتیجهٔ قابل قبول.

- [Alex] جریان «افزودن سریع کاربر + تکمیل مشخصات در نخستین ورود» پیاده شد: بک‌اند `POST /app-auth/users` فقط نام، نام خانوادگی، موبایل و نقش می‌گیرد؛ نام کاربری = شمارهٔ موبایل و رمز عبور = رمز تعیین‌شدهٔ مدیر یا رمز پیش‌فرض سیستم `vidara@12345` (`DEFAULT_PASSWORD` در `services/app_auth.py`). بازنشانی رمز توسط مدیر هم به همین رمز پیش‌فرض + پرچم `must_change_password` تغییر کرد. endpoint جدید `POST /app-auth/complete-profile` با گارد کاربر جاری، نام کاربری جدید (یکتا در سازمان)، رمز جدید (متفاوت با فعلی) و کد ملی را اجباری می‌گیرد و پرچم را برمی‌دارد. فرانت: فرم «افزودن کاربر» در `components/settings/UsersPanel.tsx` به چهار فیلد (نام/نام خانوادگی/موبایل/رمز اختیاری + نقش) خلاصه شد و اعتبارنامهٔ ساخته‌شده نمایش داده می‌شود؛ نشان «در انتظار تکمیل مشخصات» برای کاربران ناتمام در فهرست اضافه شد؛ صفحهٔ جدید `pages/CompleteProfile.tsx` در مسیر `/complete-profile` (بدون پوسته) تکمیل اجباری را می‌گیرد و پس از موفقیت به داشبورد می‌رود؛ گاردها در `pages/Login.tsx`، `components/AppShell.tsx` و `components/OrganizationSwitcher.tsx` اعمال شد تا کاربر ناتمام به هیچ بخش فضای کاری نرسد. اعتبارسنجی: `py_compile` فایل‌های بک‌اند بدون خطا، `eslint --quiet` بدون خطا و `vite build` موفق (۲۷۹۰ ماژول).

- [Alex] کد ملی به شناسهٔ اصلی هویت تبدیل شد و ورود بدون انتخاب سازمان بازنویسی گردید: (۱) `ensure_national_id_identity` در `services/app_auth.py` با نرمال‌سازی نام فارسی (`normalize_person_name`/`names_consistent`) — اگر کد ملی از قبل در هر سازمانی ثبت شده باشد، نام و نام خانوادگی باید با همان هویت سازگار باشد وگرنه ۴۰۹؛ این قاعده در `register`، `POST /users` (ساخت توسط مدیر)، `complete-profile`، `PATCH /me` و `PATCH /users/{id}` اعمال شد. (۲) `find_sibling_accounts` کد ملی را شرط اصلی فهرست فضاهای کاری همان شخص کرد (موبایل/نام کاربری فقط کلید کمکی برای حساب‌های قدیمی بدون کد ملی). (۳) ورود به جفت «نام کاربری + رمز عبور» تغییر کرد: رمز هر فضا مستقل است و همان جفت، حساب مقصد را مشخص می‌کند؛ مرحلهٔ انتخاب سازمان (`needs_organization`) از بک‌اند و فرانت حذف شد و تنها حالت ابهام (یوزر و رمز یکسان در چند فضا) با پیام راهنما رد می‌شود. (۴) `POST /switch-organization` حالا نام کاربری **و** رمز عبور فضای مقصد را اجباری می‌گیرد و دیالوگ تغییر فضا در `OrganizationSwitcher.tsx` دو فیلد گرفت. اعتبارسنجی: `py_compile` بدون خطا، `eslint --quiet` بدون خطا و `vite build` موفق.

- [Alex] باگ‌های یافت‌شده در تست E2E چندنقشی روی تولید رفع شد: (۱) دکوراتور مسیر `GET /app-auth/organizations` بالای تابع `my_organizations` از دست رفته بود و فهرست «تغییر فضای کاری» ۴۰۴ می‌داد — بازیابی شد؛ (۲) `switch-organization` به‌جای انتخاب اولین حساب سازمان مقصد، حالا حساب مقصد را با **نام کاربریِ واردشده** بین حساب‌های همان هویت پیدا می‌کند، پس شخصی که در یک فضا چند حساب (مثلاً مدیر و عضو) دارد می‌تواند با همان اعتبارنامه به همان حساب سوییچ کند.

- [Alex] باگ سوییچ چندحسابی ادامه داشت: `find_sibling_accounts` حساب‌ها را به‌ازای هر سازمان ادغام می‌کرد و در نتیجه حسابِ دوم شخص در یک فضا (مثلاً عضو کنار مدیر) از فهرست سوییچ حذف می‌شد؛ ادغام به لایهٔ نمایش (`GET /organizations`) منتقل شد و سرویس همهٔ حساب‌های همان هویت را برمی‌گرداند.

## شواهد تست E2E چندنقشی روی سرور تولید (۱۴۰۵/۰۶/۰۳ — 2026-08-25)
- ثبت دو فضا با یک کد ملی/نام/نام کاربری و رمز متفاوت → هر دو ۲۰۰ (هویت یکسان در چند فضا مجاز).
- ثبت با همان کد ملی ولی نام متفاوت → ۴۰۹ «نام و نام خانوادگی باید با کد ملی سازگار باشد» (ثبت‌نام و تکمیل مشخصات هر دو).
- ساخت عضو با فقط نام/نام خانوادگی/موبایل → ۲۰۰ با `username=موبایل`, `password=vidara@12345`, `must_change_password=true`.
- ورود با رمز پیش‌فرض → مستقیم به همان فضا؛ تکمیل مشخصات (نام کاربری/رمز/کد ملی) → ۲۰۰ و برداشته‌شدن پرچم.
- تفکیک فضا با رمز: یک نام کاربری در چند فضا، هر رمز همان فضا را باز می‌کند؛ یوزر+رمز کاملاً یکسان در دو فضا → ۴۰۰ با پیام راهنمای صریح (رفتار طراحی‌شده).
- سوییچ بین فضاها با نام کاربری+رمز همان فضا؛ نام کاربری غلط → ۴۰۴، رمز غلط → ۴۰۰، و سوییچ به حساب عضو در فضایی که شخص دو حساب دارد → ۲۰۰ با نقش همان حساب.
- `GET /app-auth/organizations` فهرست فضاهای همان کد ملی را برمی‌گرداند (هر سازمان یک ورودی).
- حذف سازمان: عبارت غلط/نام غلط → ۴۰۰؛ تأیید صحیح → ۲۰۰ با شمارش رکوردها و ۴۰۱ شدن ورود پس از آن.
- پاک‌سازی دادهٔ تکراری تست با خود endpointها انجام شد؛ دو فضای نمونهٔ چندنقشی («تست-چندنقشی-الف» با حساب مدیر+عضو و «تست-چندنقشی-ب») برای نمایش جریان روی تولید باقی ماندند.

